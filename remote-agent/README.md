# PowerTec Remote Agent

Agente local seguro para Windows em modo `simulation` ou `local_safe`.

Esta primeira entrega nao aceita comandos livres. Toda execucao passa por registry, policy engine e auditoria SQLite local.
